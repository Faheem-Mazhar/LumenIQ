# Planned Manager Tools
# Manager tools are tools the LLM can decide to use on its own depending on what user asks, keeping it simple to just database access

# For DB Access and Editing

# get_scheduled_posts(date, business_id)    # fetch posts for a specific date
# get_all_scheduled_posts(business_id)      # fetch all upcoming posts
# cancel_scheduled_post(post_id)            # cancel/delete a scheduled post

# get_competitor_list(business_id)  # fetch existing competitor accounts from DB
# get_competitor_posts(business_id) # fetch scraped competitor posts
